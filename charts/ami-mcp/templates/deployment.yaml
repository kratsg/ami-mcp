apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ami-mcp.fullname" . }}
  labels:
    {{- include "ami-mcp.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "ami-mcp.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        # Restart pods when the rendered pixi manifest changes.
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "ami-mcp.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.serviceAccount.create .Values.serviceAccount.name }}
      serviceAccountName: {{ include "ami-mcp.serviceAccountName" . }}
      {{- end }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        - name: pixi-install
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              cp /pixi-config/pixi.toml /workspace/pixi.toml
              {{- if .Values.amiMcp.pixiLockContent }}
              cp /pixi-config/pixi.lock /workspace/pixi.lock
              {{- end }}
              pixi install --manifest-path /workspace/pixi.toml
          volumeMounts:
            - name: pixi-config
              mountPath: /pixi-config
            - name: workspace
              mountPath: /workspace
          resources:
            {{- toYaml .Values.initResources | nindent 12 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          command: ["/bin/sh", "-c"]
          args:
            - |
              set -e
              eval "$(pixi shell-hook --manifest-path /workspace/pixi.toml --shell bash)"
              exec ami-mcp serve {{ include "ami-mcp.serveArgs" . }}
          env:
            - name: AMI_ENDPOINT
              value: {{ .Values.ami.endpoint | quote }}
            {{- if .Values.ami.pmgXsecPath }}
            - name: ATLAS_PMGXSEC_PATH
              value: {{ .Values.ami.pmgXsecPath | quote }}
            {{- end }}
            {{- if eq .Values.auth.mode "sharedSecret" }}
            {{- $ss := .Values.auth.sharedSecret }}
            - name: AMI_MCP_SHARED_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "ami-mcp.sharedSecretName" . }}
                  key: shared-secret
            {{- if $ss.x509.existingSecret }}
            - name: X509_USER_PROXY
              value: {{ printf "%s/%s" $ss.x509.mountPath $ss.x509.proxyKey | quote }}
            {{- end }}
            {{- if $ss.x509CertDir }}
            - name: X509_CERT_DIR
              value: {{ $ss.x509CertDir | quote }}
            {{- end }}
            {{- else }}
            - name: AMI_MCP_BROKER_URL
              value: {{ .Values.auth.broker.brokerUrl | quote }}
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.server.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: workspace
              mountPath: /workspace
            {{- if and (eq .Values.auth.mode "sharedSecret") .Values.auth.sharedSecret.x509.existingSecret }}
            - name: x509-creds
              mountPath: {{ .Values.auth.sharedSecret.x509.mountPath }}
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: pixi-config
          configMap:
            name: {{ include "ami-mcp.fullname" . }}-pixi
            items:
              - key: pixi.toml
                path: pixi.toml
              {{- if .Values.amiMcp.pixiLockContent }}
              - key: pixi.lock
                path: pixi.lock
              {{- end }}
        - name: workspace
          emptyDir: {}
        {{- if and (eq .Values.auth.mode "sharedSecret") .Values.auth.sharedSecret.x509.existingSecret }}
        - name: x509-creds
          secret:
            secretName: {{ .Values.auth.sharedSecret.x509.existingSecret }}
            defaultMode: 0400
        {{- end }}
        {{- with .Values.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
